# safeher

A new Flutter project.

## Play Store release

Before building a release bundle, add your upload keystore settings to `android/key.properties`.
Use `android/key.properties.example` as the template, then run a release build such as `flutter build appbundle` once the Flutter SDK is available in your environment.
